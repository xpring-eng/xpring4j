import { assert } from "chai";
import "mocha";

/* eslint-disable @typescript-eslint/no-magic-numbers */
/* eslint-disable @typescript-eslint/no-non-null-assertion */

describe("ILP Node Client Test", function(): void {
  it("Test test - Is this thing on?", function(): void {
    // GIVEN a tautogy WHEN the truth is asserted THEN it is true.
    assert.isTrue(true);
  });
});
